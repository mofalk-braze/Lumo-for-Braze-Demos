{
  "name": "braze-demo-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Local desktop wrapper for Braze Demo Control Room workspaces.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js",
    "pack": "electron-builder --mac dir",
    "dist": "electron-builder --mac dmg zip"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3"
  }
}
